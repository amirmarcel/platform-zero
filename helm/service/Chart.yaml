apiVersion: v2
name: service
description: >-
  Generic Helm chart for every service on platform-zero. Platform-owned:
  engineers never edit this chart. Per-service values are generated by
  `platformctl render` into helm/service/values/<name>.yaml.
type: application
version: 0.1.0
appVersion: "1.0"
